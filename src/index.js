// This is always the first file that is executed by the browser 
import ReactDOM from 'react-dom';

import './index.css';
import App from './App';


ReactDOM.render(<App />, document.getElementById('root'));
